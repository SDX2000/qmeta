#ifndef ${gaurd}
#define ${gaurd}

#include "QMetaQVariantTransformerBase.h"

class ${className} : public QMetaQVariantTransformerBase
{
public:
    ${className}(int ruleId, const QVariant& input);
    bool parse(QString& output) override;
    const ParseError *getError() const;

    enum RuleEnum {
        NEXT_RULE,
        ${ruleEnums}
    };

private:
    //Use applyRule() to access these functions
    ${ruleProtos}

private:
    void initRuleMap();
};

#endif // ${GUARD}
