- Let CHECK_POINT take only one parameter
- Optimize AST generation

* Error reporting/handling
- Collect rule args and return values in the ParseError entries.
- The ParseError entries should resemble the parse tree from which the error was generated.
    - It might be easier if the error tree is maintained within the parser.
